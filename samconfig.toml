version = 0.1

[default.deploy.parameters]
stack_name = "hallmark-dev"
resolve_s3 = true
capabilities = "CAPABILITY_IAM"
confirm_changeset = true
# region is intentionally not set here — pass with `sam deploy --region <region>`
# or set AWS_REGION in the environment. Regions are never hardcoded.
